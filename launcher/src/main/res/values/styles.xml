<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>
    <!--
        <style name="Btn" >
            <item name="layout_width">100dp</item>
            <item name="layout_height">50dp</item>
            <item name="layout_gravity">center_horizontal</item>
            <item name="gravity">center</item>
        </style>
    -->
    <style name="MainBtnStyle">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">match_parent</item>
        <item name="android:layout_marginEnd">@dimen/marg_midl</item>
        <item name="android:layout_marginStart">@dimen/marg_midl</item>
        <item name="android:layout_weight">1</item>
        <item name="android:background">@android:color/darker_gray</item>
        <item name="android:gravity">center</item>
    </style>
    </resources>
